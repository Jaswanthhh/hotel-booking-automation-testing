# Base Application URLs
app.url=https://automationintesting.online/
admin.login.url=https://automationintesting.online/admin
admin.report.url=https://automationintesting.online/admin/report

# Admin Credentials (can be overridden by environment variables)
admin.username=admin
admin.password=password

# Browser Configuration
browser=chrome
headless=false

# Timeouts in Seconds
timeout.explicit=10
timeout.pageload=30
